#include "stdafx.h"
/*

copy to dbj++ here WINFILE R&D internals
and test here

#include "./winfile_dbj/dbj_multimap.h"


#ifdef DBJ_TESTING_EXISTS

static bool dbj_winfile_testing() {
	dbj::print(": dbj winfile testing");

	winfile::internal::lock_unlock padlock_;

		static INT anchor{ 1 };
			auto rez1 = winfile::internal::is_prefix(L"abra", L"abra ka dabra");
			auto rez2 = winfile::internal::is_prefix(L"abra ka dabra", L"abra");
		return true;
}

static bool do_it_here = dbj_winfile_testing();
#endif
*/
